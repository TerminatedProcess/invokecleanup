clear
set -gx ENV_NAME '.venv'
alias rmenv='rm -rf .envrc $ENV_NAME uv.lock; exit'
alias mkenv='mkuv $ENV_NAME 3.12.10'
alias run='clear;streamlit run app.py'
alias stop='pkill -f streamlit'

function install
   clear
   uv pip install -r requirements.txt
end
